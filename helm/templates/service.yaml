apiVersion: v1
kind: Service
metadata:
  name: {{ include "ferriskey-website.fullname" . }}
  labels:
    {{- include "ferriskey-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ferriskey-website.selectorLabels" . | nindent 4 }}
