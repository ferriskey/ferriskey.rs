apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ferriskey-website.fullname" . }}-test-connection"
  labels:
    {{- include "ferriskey-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ferriskey-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
