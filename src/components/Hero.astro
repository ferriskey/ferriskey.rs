---
// Hero section - two-column layout with console preview
---

<section class="relative min-h-screen flex items-center overflow-hidden pt-20">
  <!-- Background -->
  <div class="absolute inset-0 bg-grid opacity-20"></div>

  <div class="container mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center min-h-[calc(100vh-80px)] py-16">

      <!-- Left: Text Content -->
      <div class="flex flex-col justify-center">
        <!-- Badge -->
        <div class="inline-flex items-center gap-2 mb-6 hero-badge">
          <span class="text-xs font-mono text-slate-400 uppercase tracking-widest">Open Source · Apache 2.0 · Early Access · v0.3</span>
        </div>

        <!-- Headline -->
        <h1 class="text-5xl sm:text-6xl font-bold leading-tight mb-6 hero-title">
          <span class="text-white">A Modern IAM Built</span><br/>
          <span class="text-white">for</span>
          <span class="text-gradient"> Distributed</span><br/>
          <span class="text-white">Systems</span>
        </h1>

        <!-- Subheadline -->
        <p class="text-lg text-slate-400 mb-8 max-w-lg leading-relaxed hero-sub">
          FerrisKey is an open-source, cloud-native Identity & Access Management platform designed for Kubernetes, multi-tenancy, and modern security architectures.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-wrap gap-4 hero-cta">
          <a
            href="https://docs.ferriskey.rs/en/getting-started/introduction"
            target="_blank"
            class="inline-flex items-center gap-2 bg-ferris-500 hover:bg-ferris-600 text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-200"
          >
            Get Started
          </a>
          <a
            href="https://docs.ferriskey.rs"
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center gap-2 border border-slate-700 hover:border-slate-500 text-slate-300 hover:text-white font-semibold px-6 py-3 rounded-lg transition-colors duration-200"
          >
            Read the Docs
          </a>
        </div>
      </div>

      <!-- Right: SI diagram — IAM at the center -->
      <div class="flex justify-center lg:justify-end items-center hero-diagram">
        <!--
          Tout le schéma est un seul SVG avec viewBox fixe (480×400).
          Cela garantit que les lignes partent exactement depuis les bords des boîtes.

          Layout (en unités SVG) :
          - Col gauche  : x 0–148  (nodes w=148)
          - Gutter       : 16px de chaque côté du centre
          - Centre hub   : x 172–308 (hub w=136, centré à x=240)
          - Col droite   : x 332–480 (nodes w=148)

          Nodes height=36, gap=12
          Label col : h=16
          py haut/bas : 20

          Y positions (top of each node, label inclus) :
          label top  = 20
          node1 top  = 20+16+4  = 40   → centre y = 40+18 = 58
          node2 top  = 58+18+12 = 88   → centre y = 88+18 = 106
          node3 top  = 106+18+12= 136  → centre y = 136+18= 154
          node4 top  = 154+18+12= 184  → centre y = 184+18= 202

          Total height ≈ 202+18+20 = 240   →  viewBox height = 260

          Hub centré verticalement : cx=240, cy=130, w=112, h=112
          → hub top = 74, hub bottom = 186, cy hub = 130
        -->
        <svg
          xmlns="http://www.w3.org/2000/svg"
          viewBox="0 0 480 260"
          class="w-full max-w-xl diagram-svg"
          role="img"
          aria-label="FerrisKey IAM architecture diagram"
        >
          <defs>
            <filter id="hub-glow" x="-50%" y="-50%" width="200%" height="200%">
              <feGaussianBlur stdDeviation="8" result="blur"/>
              <feMerge><feMergeNode in="blur"/><feMergeNode in="SourceGraphic"/></feMerge>
            </filter>
          </defs>

          <!-- ── Lignes de connexion (tracées après les nodes) ── -->
          <!-- longueur approximative des lignes pour stroke-dashoffset :
               L gauche ≈ 85px max, droite ≈ 85px max → on utilise 120 pour couvrir toutes -->
          <g class="anim-lines">
            <line class="line-left" x1="148" y1="58"  x2="172" y2="130" stroke="rgba(249,115,22,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-left" x1="148" y1="106" x2="172" y2="130" stroke="rgba(249,115,22,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-left" x1="148" y1="154" x2="172" y2="130" stroke="rgba(249,115,22,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-left" x1="148" y1="202" x2="172" y2="130" stroke="rgba(249,115,22,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-right" x1="308" y1="130" x2="332" y2="58"  stroke="rgba(14,165,233,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-right" x1="308" y1="130" x2="332" y2="106" stroke="rgba(14,165,233,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-right" x1="308" y1="130" x2="332" y2="154" stroke="rgba(14,165,233,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
            <line class="line-right" x1="308" y1="130" x2="332" y2="202" stroke="rgba(14,165,233,0.3)" stroke-width="1.5" stroke-dasharray="120" stroke-dashoffset="120"/>
          </g>

          <!-- ── Col gauche : Consumers ── -->
          <g class="anim-left">
            <text x="74" y="34" text-anchor="middle" font-family="ui-monospace,monospace" font-size="9" fill="#64748b" letter-spacing="1.5">CONSUMERS</text>

            <rect x="0" y="40" width="148" height="36" rx="8" fill="rgba(249,115,22,0.04)" stroke="rgba(249,115,22,0.2)" stroke-width="1"/>
            <svg x="10" y="49" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(249,115,22,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
            <text x="35" y="63" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">Web App</text>

            <rect x="0" y="88" width="148" height="36" rx="8" fill="rgba(249,115,22,0.04)" stroke="rgba(249,115,22,0.2)" stroke-width="1"/>
            <svg x="10" y="97" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(249,115,22,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"/>
            </svg>
            <text x="35" y="111" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">Mobile App</text>

            <rect x="0" y="136" width="148" height="36" rx="8" fill="rgba(249,115,22,0.04)" stroke="rgba(249,115,22,0.2)" stroke-width="1"/>
            <svg x="10" y="145" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(249,115,22,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
            </svg>
            <text x="35" y="159" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">API / CLI</text>

            <rect x="0" y="184" width="148" height="36" rx="8" fill="rgba(249,115,22,0.04)" stroke="rgba(249,115,22,0.2)" stroke-width="1"/>
            <svg x="10" y="193" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(249,115,22,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0z"/>
            </svg>
            <text x="35" y="207" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">End Users</text>
          </g>

          <!-- ── Hub FerrisKey ── -->
          <g class="anim-hub">
            <ellipse cx="240" cy="130" rx="58" ry="58" fill="rgba(249,115,22,0.12)" filter="url(#hub-glow)" class="hub-glow-pulse"/>
            <rect x="172" y="74" width="136" height="112" rx="16" fill="#0f172a" stroke="rgba(249,115,22,0.55)" stroke-width="1.5"/>
            <image href="/ferriskey.png" x="200" y="88" width="80" height="80" preserveAspectRatio="xMidYMid meet"/>
            <text x="240" y="174" text-anchor="middle" font-family="ui-monospace,monospace" font-size="9" fill="rgba(249,115,22,0.85)" font-weight="700" letter-spacing="0.5">FerrisKey IAM</text>
          </g>

          <!-- ── Col droite : Platform ── -->
          <g class="anim-right">
            <text x="406" y="34" text-anchor="middle" font-family="ui-monospace,monospace" font-size="9" fill="#64748b" letter-spacing="1.5">PLATFORM</text>

            <rect x="332" y="40" width="148" height="36" rx="8" fill="rgba(14,165,233,0.04)" stroke="rgba(14,165,233,0.2)" stroke-width="1"/>
            <svg x="342" y="49" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(14,165,233,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
            </svg>
            <text x="367" y="63" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">Kubernetes</text>

            <rect x="332" y="88" width="148" height="36" rx="8" fill="rgba(14,165,233,0.04)" stroke="rgba(14,165,233,0.2)" stroke-width="1"/>
            <svg x="342" y="97" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(14,165,233,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
            </svg>
            <text x="367" y="111" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">ArgoCD</text>

            <rect x="332" y="136" width="148" height="36" rx="8" fill="rgba(14,165,233,0.04)" stroke="rgba(14,165,233,0.2)" stroke-width="1"/>
            <svg x="342" y="145" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(14,165,233,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
            </svg>
            <text x="367" y="159" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">Prometheus</text>

            <rect x="332" y="184" width="148" height="36" rx="8" fill="rgba(14,165,233,0.04)" stroke="rgba(14,165,233,0.2)" stroke-width="1"/>
            <svg x="342" y="193" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="rgba(14,165,233,0.7)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
            <text x="367" y="207" font-family="ui-monospace,monospace" font-size="11" fill="#94a3b8" font-weight="500">OpenTelemetry</text>
          </g>

          <!-- ── Caption ── -->
          <text class="anim-caption" x="240" y="248" text-anchor="middle" font-family="ui-monospace,monospace" font-size="9" fill="#475569" letter-spacing="0.3">
            IAM as the single identity layer across your entire information system
          </text>
        </svg>
      </div>

    </div>
  </div>
</section>

<style>
  /* ════════════════════════════════
     Partie texte (colonne gauche)
     Chaque élément glisse vers le haut + fade in,
     avec un délai croissant.
  ════════════════════════════════ */
  .hero-badge {
    animation: hero-up 0.5s cubic-bezier(0.22, 1, 0.36, 1) 0s both;
  }
  .hero-title {
    animation: hero-up 0.6s cubic-bezier(0.22, 1, 0.36, 1) 0.1s both;
  }
  .hero-sub {
    animation: hero-up 0.6s cubic-bezier(0.22, 1, 0.36, 1) 0.22s both;
  }
  .hero-cta {
    animation: hero-up 0.5s cubic-bezier(0.22, 1, 0.36, 1) 0.34s both;
  }
  /* Diagramme : fade in depuis la droite, légèrement décalé */
  .hero-diagram {
    animation: hero-right 0.7s cubic-bezier(0.22, 1, 0.36, 1) 0.2s both;
  }

  @keyframes hero-up {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes hero-right {
    from { opacity: 0; transform: translateX(32px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  /* ════════════════════════════════
     Partie schéma SVG
  ════════════════════════════════ */

  /* ── Nodes gauche : glissent depuis la gauche ── */
  .anim-left {
    animation: slide-from-left 0.6s cubic-bezier(0.22, 1, 0.36, 1) 0.5s both;
  }

  /* ── Nodes droite : glissent depuis la droite ── */
  .anim-right {
    animation: slide-from-right 0.6s cubic-bezier(0.22, 1, 0.36, 1) 0.65s both;
  }

  /* ── Hub : scale + fade in ── */
  .anim-hub {
    animation: scale-in 0.5s cubic-bezier(0.34, 1.56, 0.64, 1) 0.85s both;
    transform-origin: 240px 130px;
  }

  /* ── Lignes : tracées après le hub ── */
  .anim-lines .line-left {
    animation: draw-line 0.5s ease-out 1.15s both;
  }
  .anim-lines .line-right {
    animation: draw-line 0.5s ease-out 1.3s both;
  }

  /* ── Caption : fade in en dernier ── */
  .anim-caption {
    animation: fade-in 0.4s ease-out 1.5s both;
  }

  /* ── Glow qui pulse en boucle ── */
  .hub-glow-pulse {
    animation: glow-pulse 3s ease-in-out 1.2s infinite;
  }

  @keyframes slide-from-left {
    from { opacity: 0; transform: translateX(-24px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @keyframes slide-from-right {
    from { opacity: 0; transform: translateX(24px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @keyframes scale-in {
    from { opacity: 0; transform: scale(0.75); }
    to   { opacity: 1; transform: scale(1); }
  }

  @keyframes draw-line {
    from { stroke-dashoffset: 120; opacity: 0; }
    to   { stroke-dashoffset: 0;   opacity: 1; }
  }

  @keyframes fade-in {
    from { opacity: 0; }
    to   { opacity: 1; }
  }

  @keyframes glow-pulse {
    0%, 100% { opacity: 0.7; r: 58; }
    50%       { opacity: 1;   r: 68; }
  }
</style>
